---
name: team1
on:
  push:
    branches: 
      - main
      - feature/*
    paths:
      - 'core-shared-services/team1/**'
      - '.github/workflows/team1.yml'
  pull_request:
    branches:
      - main
    types: [opened, edited, reopened, synchronize]
    paths:
      - 'core-shared-services/team1/**'
      - '.github/workflows/team1.yml'
  workflow_dispatch:
env:
  AWS_ACCESS_KEY_ID:  ${{ secrets.AWS_ACCESS_KEY_ID }}
  AWS_SECRET_ACCESS_KEY:  ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  TF_IN_AUTOMATION: true
defaults:
  run:
    shell: bash

jobs:
  plan-team1:
    name: Plan - team1
    runs-on: ubuntu-latest
    if: github.ref != 'refs/heads/main' || github.event_name == 'workflow_dispatch'
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2.3.4
      - name: Load and Configure Terraform
        uses: hashicorp/setup-terraform@v1.3.2
        with:
          terraform_version: 1.0.1
          terraform_wrapper: false
      - name: Terraform plan - team1
        run: |
          echo "Terraform plan - ${TF_ENV}"
          bash scripts/terraform-init.sh core-shared-services/team1
          terraform -chdir="core-shared-services/team1" workspace new "core-shared-services-${TF_ENV}"
          terraform -chdir="core-shared-services/team1" workspace select "core-shared-services-${TF_ENV}"
          bash scripts/terraform-plan.sh core-shared-services/team1
        env:
          TF_ENV: production

  deploy-team1:
    name: Deploy - team1
    runs-on: ubuntu-latest
    if: github.ref != 'refs/heads/main' || github.event_name == 'workflow_dispatch'
    environment:
      name: team1
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2.3.4
      - name: Load and Configure Terraform
        uses: hashicorp/setup-terraform@v1.3.2
        with:
          terraform_version: 1.0.1
          terraform_wrapper: false
      - name: Terraform apply - team1
        run: |
          echo "Terraform apply - ${TF_ENV}"
          bash scripts/terraform-init.sh core-shared-services/team1
          terraform -chdir="core-shared-services/team1" workspace new "core-shared-services-${TF_ENV}"
          terraform -chdir="core-shared-services/team1" workspace select "core-shared-services-${TF_ENV}"
          bash scripts/terraform-apply.sh core-shared-services/team1
        env:
          TF_ENV: production