---
name: core-sandbox
on:
  push:
    branches: 
      - main
      - feature/*
    paths:
      - 'core-sandbox/**'
      - '.github/workflows/core-sandbox.yml'
  pull_request:
    branches:
      - main
    types: [opened, edited, reopened, synchronize]
    paths:
      - 'core-sandbox/**'
      - '.github/workflows/core-sandbox.yml'
  workflow_dispatch:
env:
  AWS_ACCESS_KEY_ID:  ${{ secrets.AWS_ACCESS_KEY_ID }}
  AWS_SECRET_ACCESS_KEY:  ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  TF_IN_AUTOMATION: true
defaults:
  run:
    shell: bash

jobs:
  plan-core-sandbox:
    name: Plan Test - core-sandbox
    runs-on: ubuntu-latest
    if: github.ref != 'refs/heads/main' || github.event_name == 'workflow_dispatch'
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2.3.4
      - name: Load and Configure Terraform
        uses: hashicorp/setup-terraform@v1.3.2
        with:
          terraform_version: 1.0.1
          terraform_wrapper: false
      - name: Terraform plan - test
        run: |
          echo "Terraform plan - ${TF_ENV}"
          bash scripts/terraform-init.sh core-sandbox
          terraform -chdir="core-sandbox" workspace select "core-sandbox-${TF_ENV}"
          bash scripts/terraform-plan.sh core-sandbox
        env:
          TF_ENV: dev

  deploy-core-sandbox:
    name: Deploy Test - core-sandbox
    runs-on: ubuntu-latest
    if: github.ref != 'refs/heads/main' || github.event_name == 'workflow_dispatch'
    environment:
      name: core-sandbox
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2.3.4
      - name: Load and Configure Terraform
        uses: hashicorp/setup-terraform@v1.3.2
        with:
          terraform_version: 1.0.1
          terraform_wrapper: false
      - name: Terraform apply - test
        run: |
          echo "Terraform apply - ${TF_ENV}"
          bash scripts/terraform-init.sh core-sandbox
          terraform -chdir="core-sandbox" workspace select "core-sandbox-${TF_ENV}"
          bash scripts/terraform-apply.sh core-sandbox
        env:
          TF_ENV: dev