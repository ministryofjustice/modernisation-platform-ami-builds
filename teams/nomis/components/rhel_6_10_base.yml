---
name: rhel_6_10_base
description: rhel 6.10 base image component.
schemaVersion: 1.0
parameters:
  - Version:
      type: string
      default: "0.0.1"
      description: Component version (update this each time the file changes)
  - Platform:
      type: string
      default: "Linux"
      description: Platform.
phases:
  - name: build
    steps:
      - name: UpdateOS
        action: UpdateOS

      - name: InstallPackages
        action: ExecuteBash
        inputs:
          commands:
            - sudo yum install wget curl unzip git python nc ca-certificates -y
